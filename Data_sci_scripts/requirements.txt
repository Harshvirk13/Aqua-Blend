# ============================================================
# Aquablend — pinned dependencies
# Rebuilt from actual imports found in the team's data cleaning,
# validation, and Supabase upload scripts.
# Install with:  pip install -r requirements.txt
# ============================================================

# --- Data handling ---
pandas==2.2.3
numpy==1.26.4

# --- Reading Excel source files (clean_water_quality.py) ---
openpyxl==3.1.5

# --- HTTP requests (WMIS scraper scripts, JSON upload script) ---
requests==2.32.3

# --- Environment / secrets ---
python-dotenv==1.0.1

# --- Supabase client (upload scripts) ---
supabase==2.7.4

# NOTE: tkinter (used for file-picker dialogs in the validation scripts)
# is part of Python's standard library, NOT a pip package. If it's
# missing on Linux, install it via the OS package manager instead:
#   sudo apt install python3-tk
